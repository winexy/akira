name: Deploy

on:
  push:
    branches: [master]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo $HOME
          pwd
      - uses: actions/checkout@v2
      - run: pwd
      - run: yarn install --frozen-lockfile 
      - run: yarn build
      - run: ls -R

